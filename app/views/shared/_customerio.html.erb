<script type="text/javascript">
  var _cio = _cio || [];
  (function() {
    var a,b,c;a=function(f){return function(){_cio.push([f].
    concat(Array.prototype.slice.call(arguments,0)))}};b=["load","identify",
    "sidentify","track","page"];for(c=0;c<b.length;c++){_cio[b[c]]=a(b[c])};
    var t = document.createElement('script'),
        s = document.getElementsByTagName('script')[0];
    t.async = true;
    t.id    = 'cio-tracker';
    t.setAttribute('data-site-id', '<%=ENV['CUSTOMER_IO_SITE_ID']-%>');
    t.src = 'https://assets.customer.io/assets/track.js';
    s.parentNode.insertBefore(t, s);
  })();
</script>


<% if user_signed_in? -%>
  <script type="text/javascript">
   _cio.identify({
     // Required attributes
     id: <%= current_user.id -%>,
     email: '<%= current_user.email -%>',
     created_at: <%= current_user.created_at.to_i -%>,

     first_name: '<%= current_user.first_name -%>',   // Add any attributes you'd like to use in the email subject or body.
     last_name: '<%= current_user.last_name -%>',     // Add any attributes you'd like to use in the email subject or body.
     space_count: '<%= current_user.created_spaces.count -%>',
     payment_method_count: '<%= current_user.payment_methods.count -%>',
     members_count: '<%= current_user.created_members.count -%>'
   });
  </script>
<% end -%>
